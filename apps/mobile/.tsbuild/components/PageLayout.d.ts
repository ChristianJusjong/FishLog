import React, { ReactNode } from 'react';
interface PageLayoutProps {
    children: ReactNode;
    showBottomNav?: boolean;
}
/**
 * Page Layout Component
 * Provides consistent layout with BottomNavigation and DrawerMenu
 * Use this to wrap your page content
 */
export default function PageLayout({ children, showBottomNav, }: PageLayoutProps): React.JSX.Element;
export {};
//# sourceMappingURL=PageLayout.d.ts.map