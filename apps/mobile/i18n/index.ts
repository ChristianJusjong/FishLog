// Internationalization stub
export const t = (key: string): string => {
  // Return the key as-is for now (no translations)
  return key;
};

export const i18n = {
  t,
  locale: 'en',
};

export default i18n;
